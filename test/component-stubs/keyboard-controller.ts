// react-native-keyboard-controller stub — ships untranspiled native source
// that node cannot parse; keyboard avoidance is a native concern anyway.
import { View } from 'react-native';

export const KeyboardAvoidingView = View;
export const KeyboardProvider = View;
export function useKeyboardHandler() {}
