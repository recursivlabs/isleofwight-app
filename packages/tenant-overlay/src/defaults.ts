export const defaults = {
  features: {
    aiAgents: true,
    communities: true,
    protocols: true,
    billing: true,
    simulator: false,
  },
  authMethods: 'email',
  locale: 'en',
} as const;
