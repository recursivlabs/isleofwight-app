/**
 * Minds theme token overrides.
 *
 * These are merged with the network's theme config at render time,
 * giving tenants code-level control over design tokens.
 */

export const themeOverrides = {
  accentColor: '#1b85d6', // Minds blue
};
