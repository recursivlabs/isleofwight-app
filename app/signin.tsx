// Conventional compatibility alias. The static file keeps /signin out of the
// root [username] profile route while reusing the supported /sign-in screen.
export { default } from './(auth)/sign-in';
