// Conventional compatibility alias. The static file keeps /signup out of the
// root [username] profile route while reusing the supported /sign-up screen.
export { default } from './(auth)/sign-up';
